from tools import constants


def load_page_text(filepath):
	with open(filepath) as infile:
		page_text = infile.read()
	return page_text
	


def create_dataframe(value_dict):
	# TOOD: establish which variables are columns then extract values from
	# post object to make  
	return None
